machine: 
  environment: 
    CIRCLE_REVERSE_DEPENDENCIES: oauthd-service
  node:
    version: 8.10.0

general:
  branches:
    only:
      - patched

dependencies:
  pre:
    - "npm install -g grunt-cli"

test: 
  override:
    - "npm test"

deployment: 
  prod: 
    branch: patched
    commands: 
      - "./circle_ci_trigger_build"
