machine:
  environment:
    CIRCLE_REVERSE_DEPENDENCIES: oauthd-service
  node:
    version: 8.9.3

general:
  branches:
    only:
      - redsift

dependencies:
  pre:
    - "npm install -g grunt-cli coffee-script jasmine-node"

test:
  override:
    - "npm test"

deployment:
  prod:
    branch: redsift
    commands:
      - "./circle_ci_trigger_build"
