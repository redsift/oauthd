machine: 
  node:
    version: 8.10.0

general:
  branches:
    only:
      - patched

dependencies:
  pre:
    - "npm install -g grunt-cli"

test: 
  override:
    - "grunt"
    - "npm test"

deployment: 
  prod: 
    branch: patched
